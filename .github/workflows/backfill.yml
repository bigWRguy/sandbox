name: backfill

on:
  workflow_dispatch:
    inputs:
      schools:
        description: 'Optional comma-separated school IDs (for example: kansas-state) to recover immediately'
        required: false
        default: ''

concurrency:
  group: scheduled
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - run: npm test
      - name: route the browser through WARP
        run: bash scripts/warp-up.sh
      - name: run historical slices
        continue-on-error: true
        id: backfill
        env:
          X_SESSIONS: ${{ secrets.X_SESSIONS }}
          X_AUTH_TOKEN: ${{ secrets.X_AUTH_TOKEN }}
          X_CT0: ${{ secrets.X_CT0 }}
          OFFERWIRE_MODE: backfill
          OFFERWIRE_BACKFILL_DAYS: 30
          OFFERWIRE_BACKFILL_SCHOOLS: ${{ inputs.schools }}
          OFFERWIRE_BACKFILL_CHUNK_DAYS: ${{ vars.OFFERWIRE_BACKFILL_CHUNK_DAYS || '30' }}
          OFFERWIRE_BACKFILL_ANCHOR: ${{ vars.OFFERWIRE_BACKFILL_ANCHOR || '' }}
          OFFERWIRE_REQUESTS_PER_CRED: ${{ vars.OFFERWIRE_BACKFILL_REQUESTS || '36' }}
          OFFERWIRE_SCROLLS: ${{ vars.OFFERWIRE_BACKFILL_SCROLLS || '2' }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OFFERWIRE_MODEL: ${{ vars.OFFERWIRE_MODEL }}
        run: node src/pipeline.js
      - run: node scripts/build-status.mjs && node scripts/build-site.mjs
      - name: commit ledger
        run: |
          git config user.name "automation"
          git config user.email "automation@users.noreply.github.com"
          git add data/ site/wire.json site/status.json
          if ! git diff --staged --quiet; then
            git commit -m "wire backfill: $(date -u +'%Y-%m-%dT%H:%MZ')"
            git pull --rebase --autostash origin "${GITHUB_REF_NAME}"
            git push
          fi
      - name: verify collector
        if: steps.backfill.outcome == 'failure'
        run: |
          echo "The collector failed, but its state and any partial ledger writes were committed above."
          exit 1
